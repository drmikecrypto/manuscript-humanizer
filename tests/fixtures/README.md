# Test fixtures

Regression tests read `examples/demo_manuscript.md` (synthetic AI-draft benchmark).

For private manuscripts and humanized output, use local gitignored paths:

```
tests/test_manuscripts/   # your files, never committed
tests/output/             # pipeline output, never committed
```
